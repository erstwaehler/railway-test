{
  "name": "railway-test",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.8.6"
  },
  "packageManager": "bun@1.1.38",
  "engines": {
    "node": ">=18"
  }
}
